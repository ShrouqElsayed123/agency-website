/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.pexels.com",
            "i.pinimg.com", "images.unsplash.com"
        ],
    },
}

module.exports = nextConfig
